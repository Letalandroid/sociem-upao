---
import type { GetStaticPaths } from "astro";
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";

export const getStaticPaths = (async () => {
  const sessions = await getCollection("sessions");

  return sessions.map((s) => ({
    params: { id: s.slug },
    props: { s },
  }));
}) satisfies GetStaticPaths;

const { s } = Astro.props;
const { data, slug } = s;
const { id, img, course, video } = data;
---

<Layout>
  <div class="flex justify-center items-center w-full h-screen">
    <div class="flex mx-2 flex-col xl:flex-row gap-3 justify-center items-center xl:items-start">
        <img transition:name={`session-${id}`} class="w-full sm:w-1/3 xl:w-1/3" src={`${img}/s${id}.jpg`} alt={`flayer-session-${id}`} />
        <div class="flex flex-col gap-2">
          <h2 class="text-center xl:text-left">{course}</h2>
          <iframe src={video} allowfullscreen class="w-full h-[240px] xl:w-[720px] xl:h-[320px]"></iframe>
        </div>
      </div>
  </div>
</Layout>
